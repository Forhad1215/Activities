HEre I will solve some question and do a mini project to understand the java. 
Question 1 (Beginner – Variables & Input/Output)
Problem:
Write a Java program that:
Takes two integers as input from the user.
Their sum, Their difference ,Their product, Their quotient (integer division)
Answer: 
import java.util.Scanner;

public class practice {
    public static void main(String [] arg) {
       // tp take the input we have to write this line one time.
        Scanner sc = new Scanner(System.in);

        System.out.println("Enter the 1st number = ");
        int a = sc.nextInt();

        System.out.println("Enter your 2nd number = ");
        int b = sc.nextInt();

        int sum = a+b;
        int product = a*b;
        int diff = a-b;
        int quat = a/b;

        System.out.println("Sum of two numbers is = " + sum);
        System.out.println("Differeence of two numbers is = " + diff);
        System.out.println("product of two numbers is = " + product);
        System.out.println("Quotient of two numbers is = " + quat);
      
    }
}


Question 2 (Beginner – Conditions & Loops)
Problem:
Write a Java program that:

Takes a positive integer n from the user.

Prints whether the number is prime or not prime.

Requirements:

Use a loop to check divisibility.

Use an if statement for the decision.



Question 3 (Beginner → Intermediate: Arrays and Loops)
Problem:
Write a Java program that:

Asks the user how many numbers they want to enter.

Takes that many integers as input and stores them in an array.

Calculates and displays:

The largest number

The smallest number

The sum of all numbers

The average (as a double)

Hints/Requirements:

Use a for loop to fill the array.

Use another for loop to process (find max, min, sum).

Average = sum ÷ total count (cast to double).

This problem will help you practice:

Arrays

Loops

Basic math



import java.util.Scanner;

public class practice {
   public practice() {
   }

   public static void main(String[] var0) {
    Scanner sc= new Scanner(System.in);

    System.out.println("How many numbers will you input ?");
    int n = sc.nextInt();

    int [] array = new int[n];
    for(int i =0; i<n; i++){
        System.out.println("Input the number "+ (i+1));
        array[i] = sc.nextInt();    
    
    }

    //maximum
    int max= array[0];
    for(int i =0; i<n; i++){
        if(max < array[i]){
            max= array[i];
        }
    
   }
   System.out.println("Maximum of your numbers is "+ max);


   //minimum
    int min= array[0];
    for(int i =0; i<n; i++){
        if(min > array[i]){
            min= array[i];
        }
    
   }
   System.out.println("Minimum of your numbers is "+ min);
   //sum
   int sum=0;
   for(int i =0; i<n; i++){
    sum =sum + array[i];
   }
   System.out.println("The sum is "+ sum);
   // avarage
   double avg= (double)sum/n; 
   System.out.println("The avarage of your numbers is "+ avg);

}
}

Question 4 (Strings – Intermediate)
Problem:
Write a Java program that:

Takes a full sentence as input (can contain spaces).

Prints:

The sentence in uppercase

The sentence in lowercase

The number of vowels (a, e, i, o, u) in the sentence

The number of words in the sentence

Hints/Requirements:

Use nextLine() instead of nextInt() for string input.

Use .toUpperCase() and .toLowerCase() methods.

To count vowels, loop through each character.

To count words, you can split the string using split(" ").

This question helps you practice:

String methods

Loops with characters

Basic text processing

import java.util.Scanner;

public class practice {
   public practice() {
   }

   public static void main(String[] var0) {
    Scanner sc= new Scanner(System.in);
    System.out.println("Write something ");
    String a = sc.nextLine();
    System.out.println("you write: " +  a);

     //how many words
     String[] words = a.trim().split("\\s+");
    System.out.println("The number of words: " + words.length);

     
    // upper case and lower case
    System.out.println("Uppercase: " + a.toUpperCase());

    System.out.println("Lowercase: " + a.toLowerCase());
    // find vowels

      int count = 0;
     int length = a.length();
     for(int i=0 ;i<length; i++){
         char ch = a.charAt(i);
        if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u'
    || ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U') {
       
        count ++;
    }
}
     System.out.println("The vowels are "+ count);


}
}
Question 6 (Patterns – Centered Pyramid)
Write a Java program that takes an integer n and prints a centered pyramid like this:

For n = 5, the output should be:

markdown
Copy
Edit
    *
   ***
  *****
 *******
*********
Hints:
Use two nested loops:

First print spaces before stars

Then print stars

Rows: n (from 0 to n-1)

Spaces on each row: (n - i - 1)

Stars on each row: (2 * i + 1)
import java.util.Scanner;

public class practice {
   public practice() {
   }

   public static void main(String[] var0) {
    Scanner sc= new Scanner(System.in);
    System.out.println("The value of n ");
    int n = sc.nextInt();

    for(int i =0; i<n; i++){
        for(int j=0; j<n-1-i; j++){
            System.out.print(" ");
        }

        //we can do it by one loop. 
//         for (int j = 0; j < 2*i+1; j++) {
//         System.out.print("*");
// }

        for(int j=0; j<=i; j++){
            System.out.print("*");
        }
        for(int j=0 ; j<i; j++){
            System.out.print("*");
        }

        System.out.println();
    }


   }
}
   


